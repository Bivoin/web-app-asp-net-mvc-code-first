@using web_app_asp_net_mvc_code_first.Models.Entities
@model City

<h3>Город</h3>

@using (Html.BeginForm("Edit", "Cities", FormMethod.Post))
{
    @Html.ValidationSummary()
    @Html.EditorForModel()

    <input type="submit" value="Сохранить" />
    @Html.ActionLink("Отменить", "Index", "Cities")
}
