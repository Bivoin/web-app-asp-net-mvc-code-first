@using web_app_asp_net_mvc_code_first.Models.Entities
@model StoreBranch

<h3>Филиал</h3>

@using (Html.BeginForm("Create", "StoreBranches", FormMethod.Post))
{
    @Html.EditorForModel()

    <input type="submit" value="Сохранить" />
    @Html.ActionLink("Отменить", "Index", "StoreBranches")
}
